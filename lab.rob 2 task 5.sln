#include <iostream>

int main() {
    setlocale(0, ".1251");
    int a, b;

    std::cout << "Введiть перше число: ";
    std::cin >> a;
    std::cout << "Введiть друге число: ";
    std::cin >> b;

    std::cout << "Адреса першої змiнної: " << &a << std::endl;
    std::cout << "Адреса другої змiнної: " << &b << std::endl;

    for (int i = a; i <= b; ++i) {
        std::cout << -i << std::endl;
    }

    return 0;
}
